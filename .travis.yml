sudo: required
language: cpp

addons:
  apt:
    packages:
      - cmake
    update: true

compiler:
  - clang

script:
  - mkdir build && cd build && cmake .. && make

notifications:
  email:
    on_failure: change
    on_success: never
